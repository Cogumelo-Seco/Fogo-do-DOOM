module.exports = (Listener, cookie) => {
    const state = {
        fps: '0-0',
        fpsDisplay: '?',
    }
    
    //const a = (...props) => require(`./GameFunctions/a`)(state)

    const run = async () => {
        
    }

    return {
        state,
        run
    }
}